# 接口契约（所有人必须遵守，修改需通知1、5、6、7、8号）

> **1号已确认，本版本为最终版接口规范，请所有人对齐。**

## 通用约定

### 1. 接口前缀

全部接口统一前缀：`/api/v1/`

### 2. 统一响应外层包装

**所有接口返回统一使用以下外层包装，HTTP 状态码固定为 200，业务错误通过 `code` 字段区分：**

```json
{
    "code": 0,
    "msg": "success",
    "data": { }
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | int | 业务状态码，`0` 表示成功，非 `0` 表示业务错误 |
| msg | string | 状态描述，成功为 "success"，错误为具体错误信息 |
| data | object/array/null | 业务数据，成功时为具体数据，错误时为 `null` |

### 3. 业务错误码约定

| code | 含义 |
|------|------|
| 0 | 成功 |
| 1001 | 参数错误 / 缺少必填字段 |
| 1002 | 图片质量不合格（不调用模型推理，直接返回） |
| 1003 | 诊断记录不存在 |
| 5000 | 服务器内部错误 |

错误响应示例：

```json
{
    "code": 1002,
    "msg": "图片模糊，无法识别",
    "data": null
}
```

### 4. 模块职责划分

- **5号（后端）**：负责所有 `/api/v1/*` 接口的实现，对模型、知识库、数据库做适配封装
- **4号（模型）、7号（知识库）、8号（数据库）**：模块内部函数签名保持不变，由后端统一调用适配
- **6号（前端）**：按照本文档 JSON 样例进行 Mock 开发

---

## 1. 病害诊断接口

**请求地址：** POST /api/v1/diagnoses
**请求格式：** multipart/form-data

### 请求字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| image | file | 是 | 用户上传的小麦图片 |
| region | string | 是 | 地区（如"陕西省西安市"） |
| growth_stage | string | 是 | 小麦生育期（如"拔节期"、"抽穗期"） |
| field | string | 否 | 地块名称（用于标识具体田块） |

### 成功响应（code=0）

```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "diagnosis_id": "D202608150001",
        "created_at": "2026-08-15T10:30:08+08:00",
        "image_quality": {
            "passed": true,
            "code": "OK",
            "message": "图片质量合格"
        },
        "predictions": [
            {"class_id": 13, "class_name": "Yellow Rust", "display_name": "小麦条锈病", "confidence": 0.91},
            {"class_id": 3, "class_name": "Brown Rust", "display_name": "小麦叶锈病", "confidence": 0.06},
            {"class_id": 10, "class_name": "Septoria", "display_name": "壳针孢叶斑病", "confidence": 0.03}
        ],
        "reliable": true,
        "threshold": 0.75,
        "knowledge": {
            "introduction": "病害介绍",
            "symptoms": ["典型症状1", "典型症状2"],
            "prevention": ["防治建议1", "防治建议2"]
        },
        "weather_risk": {
            "level": "high",
            "summary": "未来三天环境有利于病害发展",
            "daily": []
        }
    }
}
```

### 图片质检失败响应（code=1002）

> **重要：质检失败时不调用模型推理，直接返回错误。**

```json
{
    "code": 1002,
    "msg": "图片模糊，无法识别",
    "data": {
        "image_quality": {
            "passed": false,
            "code": "BLURRY",
            "message": "图片模糊，无法识别"
        }
    }
}
```

### image_quality 字段说明

统一使用**对象结构**，不再使用布尔值：

| 字段 | 类型 | 说明 |
|------|------|------|
| passed | boolean | 图片质量是否合格（true=合格，false=不合格） |
| code | string | 状态码：`OK` / `BLURRY`（模糊）/ `TOO_DARK`（过暗）/ `NO_WHEAT`（未检测到小麦） |
| message | string | 图片质量检测的具体描述 |

### 字段责任约定

#### class_id（类别编号）

- **最终来源：** 由模型（3号 ResNet50 / 4号 ConvNeXt）输出，模型推理返回的类别索引即为 `class_id`。
- **Mock阶段约定：** 4号交付模型前，由5号（后端）临时生成 Mock ID 占位，但必须遵循以下规则：
  1. 5号需与4号确认模型的类别顺序，Mock ID 尽量与最终模型输出的索引对齐。
  2. 4号交付模型时，必须同时提供「类别映射表」（class_id → 英文类别名），5号据此替换 Mock 逻辑。
  3. 类别映射表统一放在 `docs/类别映射表.md`，由4号维护。

#### display_name（中文名称）

- **责任方：7号（知识库）** 负责维护并返回中文名称。
- 7号的知识库必须包含 `class_id → 中文名称` 的完整映射（15类病虫害），后端调用知识库获取 `display_name`。
- 5号（后端）不在适配层做中英文硬编码映射，所有中文名称以知识库返回为准。

---

## 2. 病害类别列表接口

**请求地址：** GET /api/v1/classes
**说明：** 前端获取15类病害名称列表，用于展示和筛选。

### 成功响应

```json
{
    "code": 0,
    "msg": "success",
    "data": {
        "total": 15,
        "classes": [
            {"class_id": 0, "class_name": "Healthy", "display_name": "健康"},
            {"class_id": 1, "class_name": "Powdery Mildew", "display_name": "白粉病"},
            {"class_id": 3, "class_name": "Brown Rust", "display_name": "小麦叶锈病"},
            {"class_id": 10, "class_name": "Septoria", "display_name": "壳针孢叶斑病"},
            {"class_id": 13, "class_name": "Yellow Rust", "display_name": "小麦条锈病"}
        ]
    }
}
```

> 注：以上为示例，完整15类以7号知识库和4号模型的类别映射表为准。

---

## 3. 接口清单

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/health | GET | 服务健康检查 |
| /api/v1/classes | GET | 病害类别列表（15类） |
| /api/v1/diagnoses | POST | 提交病害诊断 |
| /api/v1/diagnoses | GET | 诊断历史列表 |
| /api/v1/diagnoses/{id} | GET | 单条诊断详情 |
| /api/v1/statistics | GET | 数据统计 |

---

## 变更记录

| 日期 | 变更内容 | 提出人 |
|------|---------|--------|
| 2026-08-15 | 初始版本 | - |
| 2026-08-15 | 1. 请求字段新增 `field`；2. 明确 `class_id` 来源及Mock约定；3. 明确 `display_name` 由7号负责 | 5号 |
| 2026-08-15 | 1. `image_quality` 改为对象结构；2. 新增 `GET /api/v1/classes` 接口 | 5号 |
| 2026-08-15 | **1号确认最终版**：1. 所有响应统一外层包装 `{code,msg,data}`，HTTP固定200；2. 明确质检失败不调用模型推理；3. 明确模块职责（后端适配封装） | 1号 |
