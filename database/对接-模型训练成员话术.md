# 模型训练对接话术（2 号 → 3 号 / 4 号）

---

## 微信 / 钉钉直发版（复制即可）

> Hi 3号/4号，我是2号（数据处理），数据这边已经全部准备好了，给你们同步一下对接信息，有问题随时拉我：
>
> 1. **代码仓库**：已推送到 gitee，你们 clone 一下
>    - 地址：`https://gitee.com/zywddywz/wheat-disease-monitor.git`
>    - 项目根：`wheat-disease-monitor/`
>
> 2. **数据集获取**：找我要 `合并数据集.zip`，解压后放到 `wheat-disease-monitor/data/合并数据集/` 下即可
>    - 路径配置全流程写在 `database/路径配置说明.md`，照着做不用改代码
>
> 3. **类别规范（15 类）**：class_id、class_name、display_name 三个字段全部在 `database/class_mapping.json`，模型输出必须按这个来（接口契约要求）
>    - class_id 是字母序 0~14，训练 ImageFolder 自动生成的顺序刚好一致
>
> 4. **训练增强模板**：我已经写好了 `database/augment_online_transforms.py`，直接 import 用：
>    - train 用 `get_train_transform(224)`
>    - val/test 用 `get_val_test_transform(224)`
>    - predict 推理用 `get_predict_transform(224)`（和 val 一致）
>    - 这个在线增强是和我做的离线增强策略对齐的，不用自己再写一份
>
> 5. **离线增强说明**：train 里不足 2000 张的 10 类我已经做了线下增强，train 目录里文件名 `aug__` 开头的是增强图，原图和增强图放一起训就行，不用管
>
> 6. **少样本类重点关注**：Stem fly（茎蝇，原始398张）、Common Root Rot（根腐病，原始614张）这两类量最少，评估 Macro-F1 时可以重点看一下有没有学偏
>
> 7. **val/test 不要改**：我没对 val/test 做任何增强，评估基准是固定的，保证你们两个模型能公平对比
>
> 8. **溯源清单**：如果训练过程中发现图片质量或标注问题，直接拿文件路径查 `database/manifest.csv`，能找到来源数据源和原始类别
>
> 9. **如果需要重跑合并或增强**：
>    - 重合并：`database/merge_datasets.py`
>    - 重离线增强：`database/augment_offline_few_shot.py`
>    - 重生成所有交付物：`build_delivery.py`
>
> 有任何路径、类别、数据量的问题直接 @ 我就行。期待你们的模型结果！

---

## 会议口头版（5 分钟快速过）

> "各位好，我是 2 号，这边数据已经准备好了，和 3 号 4 号同步一下对接要点：
>
> **第一，怎么拿到数据**：git clone wheat-disease-monitor，找我要合并数据集 zip，解压放到项目根的 `data/合并数据集/`，具体步骤仓库里 `database/路径配置说明.md` 有详细图文，按步骤来就行。
>
> **第二，类别是怎么定的**：15 类，按字母序 class_id 0~14，class_name 是英文、display_name 是中文，全部写在 `class_mapping.json` 里，这个和接口契约是一一对应的，你们模型最终输出的 Top-3 预测结构要对上这个 class_id / class_name / display_name。
>
> **第三，增强怎么搞**：在线训练的 transform 我已经写好了 `augment_online_transforms.py`，直接 import 调用就行，不用再自己写一份；离线少样本的增强我已经做完了落到 train 目录了，直接训就行。
>
> **第四，评估基准**：val 和 test 我一张没动，保证你们两个模型公平对比；Stem fly 和 Common Root Rot 这两个类原样本少，重点关注一下 Macro-F1。
>
> **第五，遇到问题找谁**：数据路径、类别编号、图片来源这些找我；所有脚本也都在 database/ 里，需要重跑合并或增强随时可用。
>
> 其他有问题随时拉群讨论。"

---

## 交付物附言（写在提交内容末尾或 Issue 评论里）

> **已交付 2 号对接所需物料：**
> - ✅ 类别映射：`database/class_mapping.json`（含 class_id/class_name/display_name）
> - ✅ 统计报告：`database/processing_report.json` + `database/data_processing_report.md`
> - ✅ 溯源清单：`database/manifest.csv`（完整 42,826 条）
> - ✅ 离线增强清单：`database/manifest_augmented.csv`（7,460 条）
> - ✅ 合并脚本：`database/merge_datasets.py`
> - ✅ 在线增强模板：`database/augment_online_transforms.py`（训练直接调用）
> - ✅ 离线少样本增强：`database/augment_offline_few_shot.py`（已实际执行，结果在 data/合并数据集/train/）
> - ✅ 路径配置指南：`database/路径配置说明.md`
> - ✅ 一键重生成所有交付物：`build_delivery.py`
>
> 路径统一以 `wheat-disease-monitor/` 为项目根，`data/` 目录已加入 `.gitignore` 不入仓库，数据集找我取 zip。
